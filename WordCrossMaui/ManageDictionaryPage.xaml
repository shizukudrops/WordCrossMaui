<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WordCrossMaui.ManageDictionaryPage"
             Title="Manage Dictionaries">

    <ContentPage.Resources>
        <Style TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor"
                                        Value="{StaticResource Blue300Accent}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <Grid ColumnSpacing="10" Margin="50, 10, 50, 50">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <CollectionView Grid.Column="0" x:Name="dictList" SelectionMode="Multiple" CanReorderItems="True" Header="登録されている辞書" EmptyView="辞書が登録されていません">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="15, 0, 0, 0" Padding="8">
                        <Label Text="{Binding Name}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
        <VerticalStackLayout Grid.Column="1" Spacing="10">
            <Button Text="削除" Clicked="Remove_Clicked" />
            <Button Text="戻る" Clicked="Return_Clicked" />
        </VerticalStackLayout>
    </Grid>

</ContentPage>